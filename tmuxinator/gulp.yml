# ~/.tmuxinator/rails.yml
name: <%= @settings['n'] %>
root: .
windows:
  - vim:
      layout: main-vertical
      panes:
        - vim
        - zsh
  - git: git status
  - gulp: gulp
  - node: zsh
  - shell: zsh
